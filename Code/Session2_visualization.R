# R code for visualizing data

# Packages needed
install.packages("terra")
install.packages("devtools")

library(terra)
library(imageRy)
library(devtools)
# devtools related function to install packages from GitHub
install_github("ducciorocchini/imageRy")
